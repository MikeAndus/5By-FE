# Five-By Frontend (FE-0)

React 18 + Vite + TypeScript scaffold for the Five-By mobile-first PWA frontend.

## Local development

```bash
npm install
npm run dev
```

## Production build

```bash
npm run build
npm run preview
```

Installable PWA behavior is best verified in a production build (`npm run build && npm run preview`).
